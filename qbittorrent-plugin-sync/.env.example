# Copy to .env and fill in real values. NEVER commit a real .env file.
# The password may be supplied here OR entered at the interactive prompt.

QBT_URL=http://localhost:8080
QBT_USERNAME=admin
QBT_PASSWORD=CHANGE_ME

# Optional: override the default HTTP timeout (seconds).
# QBT_TIMEOUT=30
